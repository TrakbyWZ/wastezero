/** Default `number_format` for `customer_sequence` (8-digit zero-pad). */
export const DEFAULT_CUSTOMER_SEQUENCE_NUMBER_FORMAT = "00000000";

/** When `start_seq` is omitted or null, use this value. */
export const DEFAULT_CUSTOMER_SEQUENCE_START_SEQ = 1;

export const DEFAULT_CUSTOMER_SEQUENCE_OFFSET_SEQUENCE = 1;
